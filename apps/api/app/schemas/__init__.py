"""Pydantic request/response schemas.

Each domain owns a module here (auth, user, org, team, api_key, collection,
document, search, connector, permission, analytics). ``common`` holds shared
building blocks.
"""
